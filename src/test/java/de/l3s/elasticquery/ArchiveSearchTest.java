package de.l3s.elasticquery;

import java.util.List;
import java.io.IOException;
import java.text.ParseException;

import junit.framework.TestCase;

public class ArchiveSearchTest extends TestCase {
	
	  public void testApp() throws IOException, ParseException
	    {
	    	
	   /* 	 String query = "Angela Merkel";
	    	 
	    	 UrlElasticQuery urlQuery = new UrlElasticQuery();
	    	List<Snapshot> results = urlQuery.getResults(query, 1000, 1);
	    	
	    	for (Snapshot snap: results)
	    	{
	    		System.out.println ("Rank: " +snap.getRankAtService() +"\nFirst Capture date: " + snap.getFirstCaptureDate() + "\nLast Capture date: " + 
	    	snap.getLastCaptureDate()+ "\nTitle: " + snap.getTitle() +"\nSnippet: "+ snap.getSnippet() + "----------------\n\n");
	    	}
	    	*/
	    }

}
